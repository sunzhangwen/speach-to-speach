# Speech-to-Speech Backend Dependencies
# Install with:
#   pip install -r requirements.txt
#
# Note: torch/torchvision/torchaudio use the CUDA 13.0 index.
#       For CPU-only, remove --extra-index-url and use the default PyPI versions.

# --- PyTorch (CUDA 13.0) ---
--extra-index-url https://download.pytorch.org/whl/cu130
torch
torchvision
torchaudio

# --- Core ML ---
transformers
accelerate
safetensors
sentencepiece
huggingface_hub

# --- Audio ---
soundfile
scipy
numpy

# --- Qwen3 TTS ---
qwen-tts

# --- HuggingFace Speech-to-Speech pipeline ---
# Not on PyPI — install from GitHub:
#   pip install git+https://github.com/huggingface/speech-to-speech.git
# If GitHub is unreachable, use a mirror proxy:
#   pip install git+https://ghfast.top/https://github.com/huggingface/speech-to-speech.git
git+https://ghfast.top/https://github.com/huggingface/speech-to-speech.git

# --- Web server ---
fastapi
uvicorn[standard]
websockets

# --- Utilities ---
rich
tqdm
pyyaml
pillow
requests
pydantic
python-dotenv
colorama
